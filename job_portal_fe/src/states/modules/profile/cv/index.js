import {createSlice} from "@reduxjs/toolkit";

const cvSlice = createSlice({
    name: 'cv',
    initialState: {},
    reducers: {
    }
})

export const {
} = cvSlice.actions

export default cvSlice.reducer;
